import { ClassmeDirective } from './classme.directive';

describe('ClassmeDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassmeDirective();
    expect(directive).toBeTruthy();
  });
});
